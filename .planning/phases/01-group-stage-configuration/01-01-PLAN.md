---
phase: 1
plan: 1
type: implementation
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/league-configuration.test.cjs
  - src/types/league.ts
  - src/lib/league.ts
  - src/services/tournamentService.js
  - src/app/page.tsx
  - src/app/page.module.css
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - PERS-02
---

<objective>
Deliver the complete Phase 1 vertical slice: selectable tournament formats, persisted Group Stage configuration, stable empty groups, exact-capacity validation, and backward-compatible League behavior.
</objective>

<tasks>

<task type="auto">
  <name>Add the Group Stage domain configuration</name>
  <read_first>src/types/league.ts, src/lib/league.ts</read_first>
  <action>Add TournamentFormat, Group, and group configuration fields. Extend tournament creation and compatibility helpers without duplicating League rules.</action>
  <acceptance_criteria>TypeScript accepts both formats; default creation remains League; Group Stage creation produces stable labeled empty groups and an exact required-team count.</acceptance_criteria>
</task>

<task type="auto">
  <name>Add focused configuration regression tests</name>
  <read_first>package.json, src/lib/league.ts</read_first>
  <action>Use Node's built-in test runner to verify League defaults, Group Stage capacity, stable empty groups, and labels beyond Group Z without adding a runtime dependency.</action>
  <acceptance_criteria>`npm test` passes and catches regressions in the tournament factory and group-label generation.</acceptance_criteria>
</task>

<task type="auto">
  <name>Persist and normalize tournament formats</name>
  <read_first>src/services/tournamentService.js, src/app/page.tsx</read_first>
  <action>Normalize legacy tournaments to League at the read boundary and preserve Group Stage fields through Firebase saves.</action>
  <acceptance_criteria>Legacy documents without format load as League; saved Group Stage tournaments retain configuration after snapshot reload.</acceptance_criteria>
</task>

<task type="auto">
  <name>Build the creation and setup UI</name>
  <read_first>src/app/page.tsx, src/app/page.module.css, .planning/phases/01-group-stage-configuration/01-UI-SPEC.md</read_first>
  <action>Add the format selector, conditional numeric fields, live capacity summary, tournament format cues, and safe Group Stage setup behavior. Never call League fixture generation for Group Stage.</action>
  <acceptance_criteria>Format controls are conditional and accessible; exact capacity is visible; League creation is unchanged; Group Stage cannot accidentally generate a cross-group League schedule.</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- Inspect creation behavior for both formats and legacy fallback semantics.
</verification>

<success_criteria>
- All Phase 1 requirements are covered.
- Existing League behavior remains compatible.
- Group Stage configuration persists and reloads.
- Fixture generation is blocked safely until later Group Stage phases.
</success_criteria>
